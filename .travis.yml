language: python
python:
  - 2.7
before_install:
  - pip install coveralls
install:
  - pip install -r requirements.txt
script:
  - coverage run -m unittest discover
  - coverage report
after_success:
  - coveralls
